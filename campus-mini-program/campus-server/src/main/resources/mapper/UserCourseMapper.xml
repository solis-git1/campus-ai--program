<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.campus.mapper.UserCourseMapper">

    <select id="getById" resultType="com.campus.entity.UserCourse">
        select * from user_course where id = #{id}
    </select>

    <select id="getByUserAndCourse" resultType="com.campus.entity.UserCourse">
        select * from user_course where user_id = #{userId} and course_id = #{courseId}
    </select>

    <select id="listByUserId" resultType="com.campus.entity.UserCourse">
        select * from user_course where user_id = #{userId}
    </select>

    <insert id="insert">
        insert into user_course(user_id, course_id, note, is_custom) values(#{userId}, #{courseId}, #{note}, #{isCustom})
    </insert>

    <update id="update">
        update user_course
        <set>
            <if test="note != null and note != ''">note = #{note},</if>
            <if test="isCustom != null">is_custom = #{isCustom},</if>
        </set>
        where id = #{id}
    </update>

    <delete id="delete">
        delete from user_course where user_id = #{userId} and course_id = #{courseId}
    </delete>

    <delete id="deleteById">
        delete from user_course where id = #{id}
    </delete>

</mapper>
